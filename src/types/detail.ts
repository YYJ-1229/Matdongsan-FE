export interface FlipImageProps {
  week: string;
  name: string;
  descTitle: string;
  descDetail: string;
}

export interface FoodDetailInfo {
  title: string;
  desc: string;
}

export interface FoodIngredientInfo {
  title: string;
  data: string;
}
